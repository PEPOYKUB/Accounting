# ฐานข้อมูลจาก docker-compose.yml ที่โฟลเดอร์แม่
DATABASE_URL=postgres://postgres:accounting@localhost:5434/accounting

# โหมดสาธิต: แสดงรายชื่อบัญชีทดลองในหน้าเข้าสู่ระบบ
# ต้องตั้งเป็น false ก่อนใช้งานจริงทุกกรณี
DEMO_MODE=true
