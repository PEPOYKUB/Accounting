# สถาปัตยกรรมระบบบัญชีเต็มรูปแบบ (Double-Entry Accounting System)
### ธุรกิจบริการ · ทีมบัญชี 2-5 คน · TFRS for NPAEs

> **เวอร์ชัน 2** (แก้ไข 12 ส.ค. 2569) — ปรับจากฉบับแรกตามผลการตรวจสอบเชิงบัญชีและเทคนิค
> ฉบับเดิมเก็บไว้ที่ [`_archive/full-accounting-system-architecture.v1.md`](_archive/full-accounting-system-architecture.v1.md)
>
> **เอกสารที่เกี่ยวข้อง**
> - [`db/schema.sql`](db/schema.sql) — โครงสร้างฐานข้อมูลจริงพร้อม constraint ทั้งหมด
> - [`db/seed_chart_of_accounts.sql`](db/seed_chart_of_accounts.sql) — ผังบัญชีเริ่มต้นและการแมปบัญชี
> - [`docs/posting-rules.md`](docs/posting-rules.md) — กฎการลงบัญชีทุกเคสพร้อมตัวอย่างตัวเลข

---

## สรุปการเปลี่ยนแปลงจากฉบับที่ 1

| # | เรื่อง | ฉบับเดิม | ฉบับนี้ | เหตุผล |
|---|---|---|---|---|
| 1 | ภาษีขายของบริการ | Cr. ภาษีขาย ตอนออกใบแจ้งหนี้ | Cr. **พักภาษีขาย** ก่อน แล้วโอนตอนรับเงิน | จุดรับผิดของบริการคือวันรับเงิน ไม่ใช่วันวางบิล |
| 2 | ภาษีซื้อ | Dr. ภาษีซื้อ ตอนตั้งหนี้ | Dr. **พักภาษีซื้อ** ก่อน โอนเมื่อได้ใบกำกับ | ใช้สิทธิภาษีซื้อได้เมื่อมีใบกำกับตัวจริง |
| 3 | ภาษีหัก ณ ที่จ่ายฝั่งจ่าย | หักตอนตั้งหนี้ เจ้าหนี้ = ยอดสุทธิ | หักตอน**จ่ายเงิน** เจ้าหนี้ = ยอด**เต็ม** | WHT เกิดเมื่อจ่าย ฉบับเดิมทำให้ยอดเจ้าหนี้ต่ำกว่าจริง |
| 4 | ภาษีหัก ณ ที่จ่ายฝั่งรับ | **ไม่มีเลย** | เพิ่ม `1160 ภาษีเงินได้ถูกหัก ณ ที่จ่าย` | ธุรกิจบริการถูกลูกค้าหัก 3% ทุกครั้ง เป็นเครดิตภาษีตอนยื่น ภงด.50 |
| 5 | Payroll | มีในไดอะแกรมแต่ไม่มีในโมดูล/ตาราง | โมดูลรับ**ยอดสรุป**จากระบบภายนอก post เป็น JE ก้อนเดียว | ปิดช่องว่างที่เอกสารเดิมขัดแย้งกันเอง |
| 6 | ผังบัญชีส่วนของผู้ถือหุ้น | `3000 ทุน/กำไรสะสม` รวมกัน | แยก ทุน / กำไรสะสม / **สรุปผลกำไรขาดทุน** / สำรองตามกฎหมาย | ปิดบัญชีสิ้นปีต้องใช้บัญชีสรุปซึ่งเดิมไม่มี |
| 7 | บังคับ Dr=Cr | อ้างว่าใช้ "Database Constraint" | ใช้ **constraint trigger แบบ deferred** | CHECK constraint ทำงานทีละแถว บังคับข้ามแถวไม่ได้ |
| 8 | รายการในเอกสาร | `items JSON` | ตาราง `*_lines` แยกจริง | ต้องแยกบัญชีรายได้/อัตราภาษี/แผนก รายบรรทัด |
| 9 | การรับ-จ่ายเงิน | 1 ใบเสร็จ = 1 ใบแจ้งหนี้ | ตาราง **allocation แบบ M:N** | รับเงินก้อนเดียวปิดหลายใบเป็นเรื่องปกติ |
| 10 | ใบลดหนี้/ใบเพิ่มหนี้ | ไม่มี | เพิ่มตาราง `ar_credit_notes` | กฎหมาย VAT บังคับ ใช้ VOID แทนไม่ได้ |
| 11 | ตารางที่หายไป | ลูกค้า/ผู้ขาย, ธนาคาร, ผู้ใช้, audit log, ศูนย์ต้นทุน | เพิ่มครบทั้งหมด | โมดูลในเอกสารเดิมอ้างถึงแต่ไม่มี schema |
| 12 | ยอดยกมาตั้งต้น | ไม่มีใน roadmap | เพิ่มเป็น **Phase 0** | เป็น blocker ของการเริ่มใช้จริงกลางปี |

---

## 1. ภาพรวมระบบ

ระบบยึดหลัก **บัญชีคู่**: ทุกรายการต้อง **เดบิต = เครดิต** เสมอ และทุกอย่างต้องไหลเข้า **สมุดรายวันทั่วไป** ก่อนสรุปเป็น **บัญชีแยกประเภท** และออกเป็น **งบการเงิน**

### 1.1 สถาปัตยกรรมแบบ 5 ชั้น

```
[ชั้น 1] เอกสารต้นทาง (Source Documents)
    รายรับ:  QT → INV → RC → (CN/DN)
    รายจ่าย: PO → Bill → PV → (หนังสือรับรองหัก ณ ที่จ่าย)
    อื่น ๆ:  ยอดสรุปเงินเดือน | ทะเบียนสินทรัพย์ | รายการปรับปรุง

           ↓  Journal Engine แปลงตาม Posting Rule (docs/posting-rules.md)

[ชั้น 2] สมุดรายวัน (Journals)
    ในระบบนี้ "สมุดรายวันย่อย" ไม่ใช่ตารางแยก แต่คือมุมมองที่กรอง
    journal_entries ด้วย source_type (SALES / PURCHASE / PAYROLL / ...)

           ↓  Post (status: DRAFT → POSTED)

[ชั้น 3] บัญชีแยกประเภททั่วไป (General Ledger)
    journal_entry_lines จัดกลุ่มตาม chart_of_accounts

           ↓  สรุปยอด (fn_trial_balance / account_period_balances)

[ชั้น 4] งบทดลอง (Trial Balance)
    ยอดยกมา + เคลื่อนไหวในงวด + ยอดคงเหลือ ทุกบัญชี

           ↓  จัดกลุ่มตาม npae_report_line

[ชั้น 5] งบการเงิน (Financial Statements)
    งบกำไรขาดทุน | งบแสดงฐานะการเงิน | งบแสดงการเปลี่ยนแปลงส่วนของผู้ถือหุ้น
    (งบกระแสเงินสด — TFRS for NPAEs ไม่บังคับ แต่ระบบรองรับผ่าน cashflow_category)
```

**หัวใจสำคัญ**: แปลงเอกสาร → รายการบัญชี ให้อัตโนมัติที่สุด เพื่อไม่ให้ฝ่ายบัญชีต้องคีย์ซ้ำสองรอบ

### 1.2 แกนหลักของระบบมีเพียง 3 อย่าง

| แกน | ตาราง | บทบาท |
|---|---|---|
| **แกนโครงสร้าง** | `chart_of_accounts` | นิยามว่าเงินก้อนหนึ่งเรียกว่าอะไร |
| **แกนความจริง** | `journal_entries` + `journal_entry_lines` | **Single Source of Truth** — ยอดทุกบาทในทุกรายงานต้องมาจากที่นี่เท่านั้น |
| **แกนเวลา** | `fiscal_years` + `accounting_periods` | ควบคุมว่าช่วงเวลาไหนยังแก้ได้ / ปิดแล้ว / ล็อกถาวร |

Sub-ledger ทั้งหมด (AR / AP / สินทรัพย์ / ภาษี) **ไม่ใช่แหล่งความจริงทางบัญชี** แต่เป็นมิติเสริมที่ผูกกลับด้วย `journal_entry_id` และต้องกระทบยอดกับบัญชีคุมใน GL ได้ตลอดเวลา

---

## 2. โมดูลของระบบ

| โมดูล | หน้าที่ | เชื่อมกับ GL อย่างไร |
|---|---|---|
| **1. ผังบัญชี** | รหัสบัญชีทั้งหมด + แมปบทบาททางบัญชี (`account_mappings`) | แกนกลางที่ทุกโมดูลอ้างอิง |
| **2. สมุดรายวันทั่วไป** | บันทึกรายการบัญชีทุกรายการ | Core Ledger |
| **3. ลูกหนี้/รายรับ (AR)** | INV → RC → CN/DN, ติดตามหนังสือรับรองหัก ณ ที่จ่ายที่ได้รับ | Auto-post ตามกฎ R-1 ถึง R-6 |
| **4. เจ้าหนี้/รายจ่าย (AP)** | Bill → PV, ออกหนังสือรับรองหัก ณ ที่จ่าย | Auto-post ตามกฎ P-1 ถึง P-7 |
| **5. เงินสด/ธนาคาร** | นำเข้า statement, จับคู่รายการ, กระทบยอดรายเดือน | จับคู่กับ `journal_entry_lines` โดยตรง |
| **6. เงินเดือน** | **รับยอดสรุป**จากระบบเงินเดือนภายนอก + แนบไฟล์หลักฐาน | Post เป็น JE ก้อนเดียวต่อเดือน (กฎ PR-1) |
| **7. สินทรัพย์ถาวร** | ทะเบียนทรัพย์สิน + ค่าเสื่อมราคาอัตโนมัติ | Auto-post ทุกสิ้นเดือน กันคิดซ้ำด้วย unique key |
| **8. ภาษี** | VAT ซื้อ-ขาย, ภพ.30, ภงด.3/53, 50 ทวิ, เครดิตภาษีถูกหัก | ดึงจาก `vat_*_items` ที่สร้างตอน post |
| **9. ปิดบัญชี** | เช็กลิสต์ก่อนปิด, ปิดงวด, ปิดปี, ยกยอด | `acc.close_period()` บังคับเงื่อนไข 7 ข้อ |
| **10. งบการเงิน** | งบทดลอง, งบกำไรขาดทุน, งบแสดงฐานะการเงิน | ดึงจาก GL ผ่าน `npae_report_line` |

---

## 3. สถาปัตยกรรมทางเทคนิค

```
┌────────────────────────────────────────────────────────────────┐
│                       PRESENTATION LAYER                       │
│  Web App (React / Next.js) — เมนูแยกตามบทบาท                    │
│  · หน้ากรอกเอกสาร (INV / RC / Bill / PV / CN)                   │
│  · หน้าลงบัญชีมือ (Journal Entry Editor)                        │
│  · หน้ารายงาน (GL, งบทดลอง, งบการเงิน, รายงานภาษี)               │
│  · แดชบอร์ดกระทบยอด (บัญชีคุม vs บัญชีย่อย, ธนาคาร)              │
└──────────────────────────┬─────────────────────────────────────┘
                           │ REST API + JWT/RBAC
┌──────────────────────────▼─────────────────────────────────────┐
│                       APPLICATION LAYER                        │
│  Backend (NestJS)                                              │
│  ┌──────────────────┐┌──────────────────┐┌──────────────────┐ │
│  │ Document Service ││  Journal Engine  ││ Reporting Service│ │
│  │ INV/RC/Bill/PV   ││ Posting Rules +  ││ TB / P&L / BS /  │ │
│  │ + Numbering      ││ ตรวจ Dr=Cr       ││ Aging / VAT      │ │
│  └──────────────────┘└──────────────────┘└──────────────────┘ │
│  ┌──────────────────┐┌──────────────────┐┌──────────────────┐ │
│  │   Tax Engine     ││ Period Closing   ││ Reconciliation   │ │
│  │ VAT พัก→จริง /   ││ เช็กลิสต์ 7 ข้อ  ││ บัญชีคุม vs ย่อย │ │
│  │ WHT รับ/จ่าย     ││ + สแนปช็อตยอด    ││ + ธนาคาร         │ │
│  └──────────────────┘└──────────────────┘└──────────────────┘ │
│  ┌──────────────────┐┌──────────────────┐                     │
│  │ Payroll Importer ││ User/Role + Audit│                     │
│  │ (รับยอดสรุป)     ││ Log              │                     │
│  └──────────────────┘└──────────────────┘                     │
└──────────────────────────┬─────────────────────────────────────┘
                           │  ทุก post ต้องอยู่ใน transaction เดียว
┌──────────────────────────▼─────────────────────────────────────┐
│                          DATA LAYER                            │
│  PostgreSQL 14+  (ACID + constraint trigger เป็นด่านสุดท้าย)    │
│  · Master:      chart_of_accounts, business_partners, ...      │
│  · Core:        journal_entries, journal_entry_lines           │
│  · Sub-ledger:  ar_*, ap_*, fixed_assets, payroll_batches      │
│  · Tax:         vat_output_items, vat_input_items, wht_*       │
│  · Control:     account_period_balances, audit_log             │
│  · File:        S3 / Google Drive (เอกสารแนบ เก็บ 5 ปี)         │
└────────────────────────────────────────────────────────────────┘
```

### 3.1 ทำไม Journal Engine ต้องแยกเป็นส่วนเอกเทศ

ทุกเอกสารเมื่อถูกอนุมัติจะไม่ได้บันทึกลงตารางของตัวเองเฉย ๆ แต่ต้อง **สร้าง Journal Entry อัตโนมัติ** ตาม Posting Rule ที่กำหนดล่วงหน้า และระบบต้อง**บังคับ** `SUM(debit) = SUM(credit)` ทุกครั้งก่อนบันทึกจริง

### 3.2 ด่านป้องกันความถูกต้อง 3 ชั้น

ระบบบัญชีต่างจากระบบทั่วไปตรงที่ **ข้อมูลผิดแม้บาทเดียวคือความเสียหายถาวร** จึงต้องมีด่านซ้อนกัน

| ชั้น | ที่ตั้ง | ตัวอย่าง |
|---|---|---|
| **1. UI** | Frontend | เตือนทันทีเมื่อ Dr ≠ Cr ก่อนกดบันทึก |
| **2. Service** | Backend | ตรวจสิทธิ์, ตรวจการอนุมัติ, คำนวณ VAT/WHT, ตรวจ allocation ไม่เกินยอดค้าง |
| **3. Database** | PostgreSQL | **ด่านสุดท้ายที่โค้ดข้ามไม่ได้** — constraint trigger บังคับ Dr=Cr, ห้ามแก้ JE ที่ POSTED, ห้ามลงงวดที่ปิดแล้ว |

ชั้นที่ 3 สำคัญที่สุด เพราะสคริปต์ migration, งาน batch, หรือคนที่เข้า DB ตรง ๆ ก็ยังฝ่าไม่ได้

---

## 4. โครงสร้างฐานข้อมูล

> โครงสร้างเต็มพร้อม constraint, trigger, view และฟังก์ชันอยู่ใน [`db/schema.sql`](db/schema.sql) — ส่วนนี้สรุปเฉพาะแนวคิด

### 4.1 กติกาที่ใช้ทั้ง schema

| กติกา | เหตุผล |
|---|---|
| จำนวนเงินใช้ **`NUMERIC(15,2)`** ผ่าน domain `acc.money_amt` เท่านั้น | float/double ทำให้ยอดเพี้ยนระดับสตางค์แบบแก้ไม่ได้ — domain ช่วยกันการเผลอประกาศผิด |
| วันที่เก็บเป็น **ค.ศ.** เสมอ | แปลงเป็น พ.ศ. เฉพาะตอนแสดงผล ไม่ปนกันในฐานข้อมูล |
| อ้างบัญชีผ่าน **`account_mappings`** ไม่ hardcode รหัส | เปลี่ยนผังบัญชีได้โดยไม่ต้องแก้โค้ด |
| เอกสารทุกใบมี **`journal_entry_id`** | ตรวจสอบย้อนกลับได้เสมอว่าเอกสารนี้ลงบัญชีอะไรไว้ |
| ทุกตารางเงินมี **audit trigger** | กฎหมายบัญชีบังคับให้ตรวจสอบได้ว่าใครแก้อะไรเมื่อไหร่ |

### 4.2 ผังบัญชี

```sql
chart_of_accounts (
  id, account_code, account_name, account_name_en,
  account_type,        -- ASSET | LIABILITY | EQUITY | REVENUE | EXPENSE
  account_subtype, normal_balance, parent_account_id,
  allow_posting,       -- FALSE = บัญชีคุม/หัวข้อ ห้ามลงรายการโดยตรง
  is_contra,           -- บัญชีปรับมูลค่า เช่น ค่าเสื่อมราคาสะสม
  cashflow_category,   -- OPERATING | INVESTING | FINANCING | CASH
  npae_report_line,    -- แมปเข้าบรรทัดงบการเงินตามแบบ NPAEs
  is_active
)
```

**บัญชีที่เพิ่มจากฉบับเดิมและเป็นหัวใจของธุรกิจบริการ**

| รหัส | ชื่อบัญชี | ประเภท | ทำไมต้องมี |
|---|---|---|---|
| 1151 | พักภาษีซื้อ | สินทรัพย์ Dr | ภาษีซื้อที่ยังไม่ได้ใบกำกับตัวจริง ใช้สิทธิยังไม่ได้ |
| **1160** | **ภาษีเงินได้ถูกหัก ณ ที่จ่าย** | **สินทรัพย์ Dr** | **เงินที่ลูกค้าหักไว้ 3% ทุกครั้ง ใช้เครดิตตอนยื่น ภงด.50** |
| 2101 | พักภาษีขาย | หนี้สิน Cr | VAT ของใบแจ้งหนี้ที่ยังไม่ถึงจุดรับผิด |
| 2110 | ภาษีมูลค่าเพิ่มค้างชำระ | หนี้สิน Cr | ยอดสุทธิที่ต้องชำระตาม ภพ.30 |
| 2200/2201/2202 | WHT ค้างนำส่ง แยก ภงด.1/3/53 | หนี้สิน Cr | แต่ละแบบยื่นคนละใบ คนละกำหนด ต้องแยกยอด |
| 3110 | สำรองตามกฎหมาย | ส่วนของผู้ถือหุ้น Cr | บริษัทจำกัดต้องจัดสรร 5% จนครบ 10% ของทุน |
| **3200** | **สรุปผลกำไรขาดทุน** | **Clearing** | **บัญชีพักที่ใช้ปิดบัญชีสิ้นปี ฉบับเดิมอ้างถึงแต่ไม่มีในผังบัญชี** |
| 5195 | ผลต่างเศษสตางค์ | ค่าใช้จ่าย Dr | รองรับการปัดเศษที่หลีกเลี่ยงไม่ได้ |
| 5196 | ภาษีซื้อต้องห้าม | ค่าใช้จ่าย Dr | ค่ารับรอง ฯลฯ ที่ขอคืนไม่ได้ ต้องเป็นค่าใช้จ่าย |

### 4.3 สมุดรายวันทั่วไป

```sql
journal_entries (
  id, entry_no, entry_date,
  period_id,             -- กำหนดอัตโนมัติจาก entry_date โดย trigger (ไม่เชื่อค่าจากแอป)
  source_type,           -- SALES_INVOICE | SALES_RECEIPT | PURCHASE_BILL | ... (17 ค่า)
  source_table,          -- คู่กับ source_doc_id เสมอ (บังคับด้วย CHECK)
  source_doc_id,
  description, status,   -- DRAFT | POSTED | VOID
  reverses_entry_id,     -- ใบนี้กลับรายการของใบไหน
  reversed_by_entry_id,  -- ใบนี้ถูกใบไหนกลับรายการ
  created_by, approved_by, posted_at
)

journal_entry_lines (
  id, journal_entry_id, line_no, account_id,
  debit_amount, credit_amount,   -- CHECK: เป็นด้านใดด้านหนึ่งเท่านั้น และ > 0
  description, cost_center_id,
  partner_id,                    -- ทำ sub-ledger รายตัวลูกหนี้/เจ้าหนี้ได้จาก GL โดยตรง
  reference_doc
)
```

### 4.4 การบังคับ Dr = Cr จริง ๆ ทำอย่างไร

`CHECK constraint` ของ PostgreSQL ทำงานได้ทีละแถวเท่านั้น จึงบังคับผลรวมข้ามแถวไม่ได้ ต้องใช้:

```sql
CREATE CONSTRAINT TRIGGER trg_jel_balanced
    AFTER INSERT OR UPDATE OR DELETE ON acc.journal_entry_lines
    DEFERRABLE INITIALLY DEFERRED     -- ตรวจตอน COMMIT เท่านั้น
    FOR EACH ROW EXECUTE FUNCTION acc.fn_je_balanced();
```

ต้องเป็น **DEFERRED** เพราะระหว่างการ insert ทีละบรรทัด ยอดย่อมไม่สมดุลชั่วคราวเป็นเรื่องปกติ

ผลที่ตามมาคือ **ทุก JE ต้องสร้างเป็นสองขั้นในทรานแซกชันเดียว**: สร้างหัวใบเป็น `DRAFT` → ใส่บรรทัดให้ครบ → เปลี่ยนสถานะเป็น `POSTED`
ระบบปฏิเสธการ insert หัวใบเป็น `POSTED` โดยตรง เพราะจะไม่มีทางใส่บรรทัดได้อีก (ดู [`posting-rules.md` §9.2](docs/posting-rules.md))

### 4.5 ตารางหลักทั้งหมด

| กลุ่ม | ตาราง |
|---|---|
| ระบบ | `app_users`, `roles`, `user_roles`, `system_settings`, `audit_log`, `document_sequences` |
| แกนเวลา | `fiscal_years`, `accounting_periods` |
| แกนโครงสร้าง | `chart_of_accounts`, `account_mappings`, `cost_centers` |
| แกนความจริง | `journal_entries`, `journal_entry_lines` |
| คู่ค้า/ธนาคาร | `business_partners`, `bank_accounts` |
| ลูกหนี้ | `ar_invoices`, `ar_invoice_lines`, `ar_receipts`, `ar_receipt_allocations`, `ar_credit_notes`, `wht_received` |
| เจ้าหนี้ | `ap_bills`, `ap_bill_lines`, `ap_payments`, `ap_payment_allocations`, `wht_certificates` |
| เงินเดือน | `payroll_batches`, `payroll_batch_lines` |
| สินทรัพย์ | `fixed_assets`, `depreciation_entries` |
| ภาษี | `vat_output_items`, `vat_input_items`, `vat_filings` |
| ธนาคาร | `bank_statement_lines`, `bank_reconciliations` |
| ปิดงวด | `closing_entries`, `account_period_balances` |

---

## 5. Business Logic: Posting Rules

กฎทั้งหมด 25 กฎพร้อมตัวอย่างตัวเลขอยู่ใน [`docs/posting-rules.md`](docs/posting-rules.md) ส่วนนี้แสดงเฉพาะวงจรหลัก

### 5.1 วงจรรายรับของธุรกิจบริการ (จุดที่ต่างจากฉบับเดิมมากที่สุด)

```
[1] ออกใบแจ้งหนี้ 100,000 + VAT 7%
Dr. ลูกหนี้การค้า (1100)              107,000
    Cr. รายได้จากการให้บริการ (4000)        100,000
    Cr. พักภาษีขาย (2101)                     7,000   ← ยังไม่ใช่ภาษีขาย

[2] รับชำระเงิน (ลูกค้าหัก ณ ที่จ่าย 3% จากฐาน 100,000)
Dr. เงินฝากธนาคาร (1020)              104,000
Dr. ภาษีถูกหัก ณ ที่จ่าย (1160)          3,000   ← สินทรัพย์ ใช้เครดิตตอนยื่น ภงด.50
    Cr. ลูกหนี้การค้า (1100)                107,000
Dr. พักภาษีขาย (2101)                   7,000
    Cr. ภาษีขาย (2100)                        7,000   ← ถึงจุดรับผิดแล้ว เข้ารายงานภาษีขาย
```

### 5.2 วงจรรายจ่ายพร้อมหักภาษี ณ ที่จ่าย

```
[1] ตั้งหนี้ค่าจ้างผู้รับเหมาช่วง 50,000 + VAT 7%
Dr. ค่าจ้างผู้รับเหมาช่วง (5020)        50,000
Dr. พักภาษีซื้อ (1151)                   3,500
    Cr. เจ้าหนี้การค้า (2000)                 53,500   ← เต็มจำนวน ไม่หัก WHT ตรงนี้

[2] จ่ายชำระ (หัก ณ ที่จ่าย 3%)
Dr. เจ้าหนี้การค้า (2000)               53,500
    Cr. ภงด.53 ค้างนำส่ง (2202)                1,500
    Cr. เงินฝากธนาคาร (1020)                  52,000
Dr. ภาษีซื้อ (1150)                      3,500
    Cr. พักภาษีซื้อ (1151)                     3,500   ← ได้ใบกำกับแล้ว ใช้สิทธิได้
```

### 5.3 เงินเดือน (รับยอดสรุป)

```
Dr. เงินเดือนและค่าแรง (5110)          300,000
Dr. ประกันสังคม-นายจ้าง (5112)           7,500
    Cr. ภงด.1 ค้างนำส่ง (2200)                 8,000
    Cr. ประกันสังคมค้างนำส่ง (2210)           15,000   ← ทั้งสองฝ่ายรวมกัน
    Cr. เงินฝากธนาคาร (1020)                 284,500
```

### 5.4 ปิดบัญชีสิ้นปี

```
[1] Dr. รายได้ทุกบัญชี (4xxx)  → Cr. สรุปผลกำไรขาดทุน (3200)
[2] Dr. สรุปผลกำไรขาดทุน (3200) → Cr. ค่าใช้จ่ายทุกบัญชี (5xxx)
[3] Dr. สรุปผลกำไรขาดทุน (3200) → Cr. กำไรสะสม (3100)      -- ขาดทุนกลับด้าน
[4] Dr. กำไรสะสม (3100)         → Cr. สำรองตามกฎหมาย (3110) -- 5% จนครบ 10% ของทุน
```
หลังขั้นที่ 3 บัญชี `3200` ต้องเป็นศูนย์เสมอ

### 5.5 กฎการตรวจสอบ

| ชั้น | จำนวนกฎ | ตัวอย่าง |
|---|---|---|
| **ฐานข้อมูล** (โค้ดข้ามไม่ได้) | 13 กฎ | Dr=Cr, ห้ามแก้ JE ที่ POSTED, ห้ามลงงวดที่ปิด, งวดห้ามซ้อนทับ, ค่าเสื่อมห้ามคิดซ้ำ, ผู้อนุมัติ ≠ ผู้บันทึก |
| **แอปพลิเคชัน** | 8 กฎ | WHT คิดจากฐานก่อน VAT, allocation ไม่เกินยอดค้าง, บัญชีคุม = บัญชีย่อย, ผลต่างปัดเศษไม่เกิน tolerance |

รายละเอียดครบใน [`docs/posting-rules.md` §9](docs/posting-rules.md)

---

## 6. รายงานที่ระบบต้องสร้างได้

| รายงาน | แหล่งข้อมูล / สูตร |
|---|---|
| **งบทดลอง** | `acc.fn_trial_balance(period_id)` — ยอดยกมา + เคลื่อนไหว + ยอดคงเหลือ |
| **งบกำไรขาดทุน** | รายได้ (4xxx) − ค่าใช้จ่าย (5xxx) จัดกลุ่มตาม `npae_report_line` |
| **งบแสดงฐานะการเงิน** | สินทรัพย์ (1xxx) = หนี้สิน (2xxx) + ส่วนของผู้ถือหุ้น (3xxx) |
| **งบแสดงการเปลี่ยนแปลงส่วนของผู้ถือหุ้น** | ยอดต้นงวด + กำไรสุทธิ − เงินปันผล ± จัดสรรสำรอง |
| **งบกระแสเงินสด** | วิธีทางอ้อม จัดกลุ่มด้วย `cashflow_category` *(NPAEs ไม่บังคับ)* |
| **บัญชีแยกประเภทรายบัญชี** | `v_gl_movements` กรองรายบัญชี พร้อมยอดยกมา |
| **AR / AP Aging** | `v_ar_aging` — Current, 1-30, 31-60, 61-90, 90+ |
| **รายงานภาษีขาย** | `vat_output_items` — รูปแบบตามประกาศอธิบดี (ลำดับที่, วันที่, เลขที่ใบกำกับ, ชื่อ, เลขผู้เสียภาษี, สาขา) |
| **รายงานภาษีซื้อ** | `vat_input_items` — แยกภาษีซื้อต้องห้ามด้วย `is_claimable` |
| **ภพ.30** | `vat_filings` |
| **หนังสือรับรองหัก ณ ที่จ่าย / ภงด.3, 53** | `wht_certificates` |
| **ทะเบียนภาษีถูกหัก ณ ที่จ่าย** | `wht_received` — ติดตามหนังสือรับรองที่ยังไม่ได้รับจากลูกค้า |
| **รายงานกระทบยอดบัญชีคุม** | `v_control_reconciliation` — **ต้องเป็นศูนย์เสมอ** |
| **รายงานตรวจสมการบัญชี** | `v_accounting_equation_check` — **ต้องเป็นศูนย์เสมอ** |

สองรายงานสุดท้ายเป็นเครื่องมือเตือนภัย ควรรันอัตโนมัติทุกวันและแจ้งเตือนทันทีที่ไม่เป็นศูนย์

---

## 7. Roles & Permissions

| บทบาท | สิทธิ์ |
|---|---|
| **ผู้จัดการบัญชี (Controller)** | ปิดงวด, แก้ผังบัญชี, อนุมัติ JE ทุกประเภท, ดูงบการเงิน, เปิดงวดที่ปิดแล้ว |
| **นักบัญชีอาวุโส** | สร้าง/แก้ JE (ยังไม่ post), บันทึกค่าเสื่อมราคา, กระทบยอดธนาคาร, นำเข้ายอดเงินเดือน |
| **นักบัญชี AR/AP** | ออก INV/Bill, บันทึกรับ-จ่ายเงิน — **แก้ผังบัญชีและปิดงวดไม่ได้** |
| **เจ้าของ/ผู้บริหาร** | ดูรายงานและแดชบอร์ดเท่านั้น (Read-only) |
| **ผู้ตรวจสอบบัญชี** | Read-only ทุกอย่าง + เข้าถึง Audit Log |

**การแบ่งแยกหน้าที่ (Segregation of Duties)**: ระบบบังคับ `created_by <> approved_by` ที่ระดับฐานข้อมูล
ทีมที่มีคนเดียวสามารถปิดกฎนี้ได้ผ่าน `system_settings.ENFORCE_MAKER_CHECKER = false` แต่ต้องรับทราบว่าเป็นจุดอ่อนที่ผู้สอบบัญชีจะตั้งข้อสังเกต

---

## 8. เทคโนโลยีที่แนะนำ

| ส่วนประกอบ | แนะนำ | เหตุผล |
|---|---|---|
| Database | **PostgreSQL 14+** | ต้องการ ACID, constraint trigger แบบ deferred, `NUMERIC` และ EXCLUDE constraint ครบ |
| Backend | **NestJS (Node.js)** | เหมาะกับ Domain-driven design แบบ Journal Engine / Posting Rule |
| ORM | **Prisma** | ต้องใช้ `$transaction()` แบบ interactive ทุกครั้งที่ post และ map เงินเป็น `Decimal` **ห้ามใช้ `Float`** |
| Frontend | React + Next.js | ฟอร์มและตารางรายงานจำนวนมาก |
| Export | Puppeteer (PDF) + ExcelJS | งบการเงิน, 50 ทวิ, รายงานภาษี |
| Auth | JWT + RBAC | หลายบทบาทตาม §7 |
| Audit | Trigger-based audit table | ติดตั้งแล้วใน `schema.sql` |
| Hosting | Render / Railway + Neon หรือ Supabase | Production-grade PostgreSQL ในงบ SME |
| Backup | Point-in-time recovery + สำเนารายวันนอก region | **พ.ร.บ.การบัญชี 2543 บังคับเก็บเอกสาร 5 ปี** |

> **ข้อควรระวังที่ยังคงไว้จากฉบับเดิม**: อย่าใช้ Google Sheets เป็นฐานข้อมูลหลัก — ไม่มี transaction แบบ atomic และไม่มีทางบังคับ Dr=Cr ได้จริง เหมาะกับ MVP/Demo เท่านั้น

---

## 9. แผนพัฒนา (Development Roadmap)

| Phase | สิ่งที่ต้องทำ | Output ที่ตรวจรับได้ |
|---|---|---|
| **Phase 0: ตั้งต้น** | ผังบัญชีจริงของกิจการ, ปีบัญชี/งวด, ผู้ใช้และสิทธิ์, **นำเข้ายอดยกมาทุกบัญชี + รายตัวลูกหนี้/เจ้าหนี้ค้าง** | งบทดลอง ณ วันเริ่มระบบตรงกับงบเดิมทุกบาท |
| **Phase 1: รากฐาน** | `journal_entries` + trigger ทั้ง 4 ตัว, ลงบัญชีมือ, **งบทดลอง** | ลง JE มือได้ ระบบปฏิเสธรายการที่ไม่สมดุลจริง |
| **Phase 2: AR/AP + ธนาคาร** | INV/RC/CN, Bill/PV, allocation แบบ M:N, พักภาษีซื้อ-ขาย, WHT สองฝั่ง, **กระทบยอดธนาคาร** | เอกสารขาย-ซื้อ auto-post และบัญชีคุมตรงกับบัญชีย่อย |
| **Phase 3: ภาษี** | Tax Engine, `vat_*_items`, ภพ.30, หนังสือรับรองหัก ณ ที่จ่าย, ภงด.3/53 | ออกรายงานภาษีซื้อ-ขายตามแบบสรรพากรและกระทบกับ GL ได้ |
| **Phase 4: สินทรัพย์ + เงินเดือน** | ทะเบียนทรัพย์สิน, ค่าเสื่อมอัตโนมัติ, นำเข้ายอดสรุปเงินเดือน | ค่าเสื่อมและเงินเดือน post อัตโนมัติทุกสิ้นเดือน |
| **Phase 5: ปิดงวดและงบการเงิน** | `close_period()` พร้อมเช็กลิสต์ 7 ข้อ, ปิดปี, งบกำไรขาดทุน, งบแสดงฐานะการเงิน | ปิดงวดได้และออกงบตามแบบ NPAEs |
| **Phase 6: เสริมความแข็งแรง** | แดชบอร์ด, รายงานเตือนภัยอัตโนมัติ, งบกระแสเงินสด, e-Tax Invoice (ถ้าต้องการ) | พร้อมใช้งานจริงระดับ SME |

**เหตุผลที่ปรับจากฉบับเดิม 3 จุด**
1. เพิ่ม **Phase 0** — ถ้าเริ่มใช้ระบบกลางปีโดยไม่มียอดยกมา ระบบจะใช้งานจริงไม่ได้เลย
2. ย้าย **งบทดลองมาไว้ Phase 1** — ถ้าไม่มีเครื่องมือตรวจตั้งแต่แรก จะไม่มีทางรู้ว่า Journal Engine ทำงานถูกหรือไม่
3. ย้าย **กระทบยอดธนาคารมาไว้ Phase 2** — เป็นงานที่ทีมบัญชีทำทุกเดือน ไม่ใช่ของเสริม

---

## 10. ความแตกต่างจากระบบ "รวม Template บัญชี" เดิม

| หัวข้อ | ระบบ Template เดิม | ระบบบัญชีเต็มรูปแบบ |
|---|---|---|
| หลักการ | บันทึกรายรับ-รายจ่ายแยกตาราง แล้วสรุปภาษี | ทุกอย่างผ่านสมุดรายวันแบบ Dr=Cr เสมอ |
| งบการเงิน | ไม่มี (มีแค่สรุปภาษี) | งบกำไรขาดทุน, งบแสดงฐานะการเงิน, งบแสดงการเปลี่ยนแปลงส่วนของผู้ถือหุ้น |
| ผังบัญชี | ไม่มี | เป็นแกนกลางของระบบทั้งหมด |
| ความถูกต้อง | พึ่งพาสูตรใน Excel/Sheet | บังคับด้วย constraint trigger ในฐานข้อมูล ที่โค้ดข้ามไม่ได้ |
| จุดรับผิดทางภาษี | ลง VAT ตอนออกเอกสาร | แยกพักภาษี/ภาษีจริงตามจุดรับผิดที่กฎหมายกำหนด |
| ภาษีถูกหัก ณ ที่จ่าย | ไม่ได้ติดตาม | เป็นสินทรัพย์ที่ติดตามได้รายใบ ใช้เครดิตตอนยื่น ภงด.50 |
| การปิดงวด | ไม่มีแนวคิดนี้ | มี Closing Entry + เช็กลิสต์ 7 ข้อ + ล็อกป้องกันแก้ย้อนหลัง |
| การตรวจสอบ | ตรวจด้วยตา | บัญชีคุม vs บัญชีย่อย และสมการบัญชี ตรวจอัตโนมัติทุกวัน |
| เหมาะกับ | ธุรกิจเล็กมาก ต้องการแค่สรุปภาษี | SME ที่ต้องยื่นงบการเงินกับกรมพัฒนาธุรกิจการค้าและสรรพากร |

---

## 11. การทดสอบ

ชุดทดสอบอยู่ที่ [`db/tests.sql`](db/tests.sql) — **รันจริงผ่านแล้ว 47/47 บน PostgreSQL 16.14**

```bash
docker run -d --name acc-test-pg -e POSTGRES_PASSWORD=acctest -e POSTGRES_DB=accounting postgres:16
```

จากนั้นรันสามไฟล์ตามลำดับ `schema.sql` → `seed_chart_of_accounts.sql` → `tests.sql`
(`tests.sql` คืน exit code ไม่เป็นศูนย์ถ้ามีข้อใดไม่ผ่าน จึงใส่ใน CI ได้ทันที)

| กลุ่ม | จำนวน | ครอบคลุม |
|---|---|---|
| กฎเหล็กระดับฐานข้อมูล | 12 | Dr=Cr, ห้ามแก้/ลบใบที่ POSTED, งวดที่ปิด, บัญชีคุม, ผู้อนุมัติ ≠ ผู้บันทึก, งวดซ้อนทับ |
| วงจรรายรับเต็มรอบ | 8 | ออกใบแจ้งหนี้ → รับชำระ → พักภาษีขายโอนเข้าภาษีขาย → ภาษีถูกหัก ณ ที่จ่าย |
| วงจรรายจ่ายเต็มรอบ | 7 | ตั้งหนี้ → จ่าย + หัก ณ ที่จ่าย → พักภาษีซื้อโอนเข้าภาษีซื้อ |
| เงินเดือน / สินทรัพย์ / เลขที่เอกสาร | 7 | ยอดสรุปเงินเดือน, ค่าเสื่อมห้ามคิดซ้ำ, ออกเลข 100 ใบไม่ซ้ำไม่ข้าม |
| ความถูกต้องรวม | 13 | สมการบัญชี, บัญชีคุม vs บัญชีย่อย, งบทดลอง, ปิดบัญชีสิ้นปี, ปิดงวด, audit log |

ผลที่ยืนยันด้วยตัวเลขจริง: รับชำระแล้วลูกหนี้และพักภาษีขายเป็น 0 · ภาษีถูกหัก ณ ที่จ่ายเป็นสินทรัพย์ 3,000 · จ่ายชำระแล้วเจ้าหนี้และพักภาษีซื้อเป็น 0 · หลังปิดบัญชีสิ้นปีบัญชีสรุปผลกำไรขาดทุนเป็น 0 · รายการที่ไม่สมดุลถูก rollback ทั้งใบที่จังหวะ COMMIT

---

## 12. ข้อจำกัดและขอบเขตที่ยังไม่ครอบคลุม

ระบุไว้อย่างตรงไปตรงมาเพื่อไม่ให้เข้าใจผิดว่าเอกสารนี้ครอบคลุมทุกเรื่อง

| เรื่อง | สถานะ | หมายเหตุ |
|---|---|---|
| สินค้าคงคลังและต้นทุนขาย | **ไม่รวม** | ออกแบบสำหรับธุรกิจบริการ ถ้าขายสินค้าในอนาคตต้องเพิ่ม Inventory + COGS ทั้งโมดูล |
| ภงด.1 และ 50 ทวิ ของพนักงาน | **บางส่วน** | ระบบรับยอดสรุป จึงออกแบบยื่นเองไม่ได้ ต้องออกจากระบบเงินเดือนต้นทาง หรือกรอก `payroll_batch_lines` เพิ่ม |
| หลายสกุลเงิน (Multi-currency) | ไม่รวม | ถ้ามีลูกค้าต่างประเทศต้องเพิ่มอัตราแลกเปลี่ยนและผลต่างอัตราแลกเปลี่ยน |
| ภงด.54 / ภพ.36 | ไม่รวม | สำหรับการจ่ายเงินไปต่างประเทศ |
| e-Tax Invoice / e-Receipt | ไม่รวม | โครงสร้างเผื่อไว้แล้ว (`tax_invoice_no`, `pdf_url`) แต่ยังไม่มีการเชื่อมต่อสรรพากร |
| งบการเงินรวม (Consolidation) | ไม่รวม | รองรับกิจการเดียว มี `cost_centers` สำหรับแยกแผนก/สาขาเท่านั้น |
| งบประมาณ (Budgeting) | ไม่รวม | ขยายได้ในอนาคต |
| การรับรู้รายได้ตามขั้นความสำเร็จ | บางส่วน | มี `1120 รายได้ค้างรับ` รองรับ แต่ยังไม่มีโมดูลบริหารโครงการเต็มรูปแบบ |

### 12.1 เอกสารในระบบ Template เดิมที่ยังไม่มีตารางรองรับ

เทียบกับชุดฟอร์มของระบบ Template เดิม พบว่ายังขาดเอกสาร 3 ประเภท ปัจจุบันเก็บเป็นข้อความอ้างอิงหรือมีแค่บัญชีใน GL

| เอกสารเดิม | สถานะปัจจุบัน | สิ่งที่ต้องเพิ่มถ้าจะใช้งานเต็มรูปแบบ |
|---|---|---|
| **ใบเสนอราคา (QT)** | มีแค่ `ar_invoices.quotation_ref` เป็นข้อความ | ตาราง `quotations` + `quotation_lines` และการแปลง QT → INV |
| **ใบสั่งซื้อ (PO)** | มีแค่ `ap_bills.po_ref` เป็นข้อความ | ตาราง `purchase_orders` + `po_lines` และการจับคู่ PO กับใบตั้งหนี้ |
| **เงินทดรองจ่าย / ใบเคลียร์** | มีบัญชี `1135` แต่ไม่มีตารางเอกสาร | ตาราง `employee_advances` + `advance_clearings` เพื่อติดตามรายคนและรายครั้ง |

ทั้งสามอย่างเป็นเอกสาร **ก่อนถึงจุดลงบัญชี** จึงไม่กระทบความถูกต้องของ GL และเลื่อนไปทำภายหลังได้
แต่ถ้าต้องการแทนที่ระบบ Template เดิมทั้งหมด ต้องเพิ่มให้ครบ มิฉะนั้นทีมจะยังต้องใช้สองระบบคู่กัน

**เอกสารเดิมที่รองรับครบแล้ว**: ใบแจ้งหนี้/ใบวางบิล → `ar_invoices` · ใบเสร็จ/ใบกำกับภาษี → `ar_receipts` · ใบสำคัญจ่าย vendor → `ap_bills` + `ap_payments` · สรุป VAT ขาย-ซื้อ → `vat_output_items` / `vat_input_items` · สรุป ภงด.3, 53 และ 50 ทวิ → `wht_certificates`

---

## หมายเหตุ

เอกสารนี้ออกแบบสำหรับระบบบัญชีคู่มาตรฐาน เหมาะกับธุรกิจบริการขนาด SME ที่มีทีมบัญชี 2-5 คน จดทะเบียนภาษีมูลค่าเพิ่ม และต้องยื่นงบการเงินตาม TFRS for NPAEs

**ก่อนเริ่มพัฒนา ควรให้ผู้ทำบัญชีหรือผู้สอบบัญชีของกิจการทบทวน [`docs/posting-rules.md`](docs/posting-rules.md) และผังบัญชีใน [`db/seed_chart_of_accounts.sql`](db/seed_chart_of_accounts.sql) ก่อน** เพราะรายละเอียดบางส่วน เช่น อัตราหัก ณ ที่จ่ายรายประเภทเงินได้ นโยบายค่าเสื่อมราคา และการจัดกลุ่มบรรทัดงบการเงิน ขึ้นกับลักษณะธุรกิจและแนวปฏิบัติที่กิจการใช้อยู่
